[tool.poetry]
name = "wriveted-api"
version = "0.1.0"
description = ""
authors = ["Brian Thorne <brian@hardbyte.nz>"]
packages = [
    {include = "app"}
]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.70"
uvicorn = {extras = ["standard"], version = "^0.16"}
alembic = "^1.7.5"
structlog = "^21.5.0"
cloud-sql-python-connector = {extras = ["pg8000"], version = "^0.4.3"}
psycopg2-binary = "^2.9.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
